apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keep-admin.fullname" . }}-test-connection"
  labels:
    {{- include "keep-admin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keep-admin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
